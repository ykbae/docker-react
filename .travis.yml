language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t ybaeofco/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ybaeofco/docker-react npm run test